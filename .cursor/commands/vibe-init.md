# vibe-init

this mcp source is in this repo and has docs here @.vibemd and the saas control plane for it is in `~/ai.5pm.labs/labs-saas-ctrl` which has it's own `.vibemd/` docs; they both share the db model with tight RLS. there are 3 environments, 1 local (currently running), 1 cloud `gamma.5pm.ai` as dev, and 1 cloud `mcp.5pm.ai` as prod.

what would it take to support the following is it possible:
